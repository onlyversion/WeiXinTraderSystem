<UserControl x:Class="Gss.PopUpWindow.AuthorityModule.ClerkAuthorityPart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <GroupBox Margin="3" >
        <GroupBox.Header>
            <CheckBox Content="提货受理" Width="Auto" IsChecked="{Binding TiHuoShouLi}"/>
        </GroupBox.Header>
        <WrapPanel>
            <CheckBox Content="绑定账号" IsChecked="{Binding BangDingUser}"/>
            <CheckBox Content="提货" IsChecked="{Binding TiHuo}"/>
            <CheckBox Content="受理明细" IsChecked="{Binding ShouLiMingXi}"/>
        </WrapPanel>
    </GroupBox>
</UserControl>
